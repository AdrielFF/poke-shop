import { makeStyles } from '@material-ui/core/styles'

export const useStyles = makeStyles({
  mainWrapper: {
    width: '100%',
    maxWidth: 1728,
    margin: '0 auto',
    padding: '6em 0',
    display: 'flex',
    flexDirection: 'column'
  },
  paper: {
    height: '100%'
  }
})